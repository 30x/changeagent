package protobufs;

message PurgeConfig {
  required int64 minDuration = 1;
  required uint32 minRecords = 2;
}

message ConfigPb {
  optional PurgeConfig purge = 1;
  required int64 heartbeatTimeout = 2;
  required int64 electionTimeout = 3;
}
