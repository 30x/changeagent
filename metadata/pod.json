{
	"apiVersion": "v1",
	"kind": "Pod",
	"metadata": {
		"name": "${APP_NAME}",
    "labels": {
        "app": "${APP_NAME}"
    },
		"annotations": {
      "publicPaths": "8080:/changes",
			"projectcalico.org/policy": "allow tcp"
		}
	},
	"spec": {
		"imagePullSecrets": [{
			"name": "ecr-key"
		}],
		"containers": [{
			"name": "${APP_NAME}",
			"image": "${APP_NAME}:latest",
			"ports": [{
				"containerPort": 8080
			}]
		}]
	}
}
